apiVersion: v1
kind: Secret
metadata:
  name: llm-gui-secrets
  labels:
    app: llm-gui
type: Opaque
# Replace placeholder values with base64-encoded secrets:
#   echo -n 'your-value' | base64
stringData:
  OPENAI_API_KEY: ""
  # Uncomment if using OIDC:
  # OIDC_CLIENT_SECRET: ""
