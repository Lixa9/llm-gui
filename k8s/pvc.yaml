apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llm-gui-data
  labels:
    app: llm-gui
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: standard  # Uncomment and set for your cluster's storage class
