apiVersion: v1
kind: Service
metadata:
  name: llm-gui
  labels:
    app: llm-gui
spec:
  type: ClusterIP
  selector:
    app: llm-gui
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
